{% extends 'base.html.twig' %}

{% block title %}EdificiosPisos{% endblock %}

{% block body %}
    <h1>EdificiosPisos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ edificios_piso.id }}</td>
            </tr>
            <tr>
                <th>Piso</th>
                <td>{{ edificios_piso.piso }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('edificios_pisos_index') }}">back to list</a>

    <a href="{{ path('edificios_pisos_edit', {'id': edificios_piso.id}) }}">edit</a>

    {{ include('edificios_pisos/_delete_form.html.twig') }}
{% endblock %}
