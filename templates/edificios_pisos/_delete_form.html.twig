<form method="post" action="{{ path('edificios_pisos_delete', {'id': edificios_piso.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ edificios_piso.id) }}">
    <button class="btn">Delete</button>
</form>
