{% extends 'base.html.twig' %}

{% block cardheader %}
	<div class="col-sm-8">
		<h5><i class="bi bi-columns"></i> Aulas</h5>
	</div>
    <div class="col-sm-4">
        <a href="{{ path('aulas_new') }}" class="btn btn-sm btn-outline-secondary" role="button"><i class="bi bi-plus-square"></i> Nueva</a>
    </div>
{% endblock %}
{% block cardbody %}
    <table class="table table-sm table-striped table-borderless">
        <thead>
            <tr>
                <th>Id</th>
                <th>Aula</th>
                <th>Capacidad</th>
                <th>Edificio</th>
                <th>acciones</th>
            </tr>
        </thead>
        <tbody>
        {% for aula in aulas %}
            <tr>
                <td>{{ aula.id }}</td>
                <td>{{ aula.aula }}</td>             
                <td>{{ aula.capacidad }}</td>
                <td>{{ aula.IdEdificio.edificio }}</td>
                <td>
                  {# <a class="btn btn-outline-primary btn-sm" role="button" href="{{ path('aulas_show', {'id': aula.id}) }}">Ver</a> #}
                  {% if is_granted("ROLE_ROOT") %}
                  <a href="{{ path('aulas_edit', {'id': aula.id}) }}"><i class="bi bi-pencil-square"></i></a>
                  {% endif %}
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">sin registros</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}