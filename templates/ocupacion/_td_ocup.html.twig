<td class="bg-light" colspan="{{bloques}}" style="transform: rotate(0);">
	<div class="text-verdelogo lh-1">
		<strong>{{ocup.IdCatedra }}</strong>
	</div>
	<div class="text-primary">Comisión:{{ocup.comision}}</div>
	<div class="text-info">{{ocup.IdArea}}</div>
	<div class="text-danger align-bottom fw-bold">
		<small>{{inicio}}&nbsp;a&nbsp;{{fin}}</small>
	</div>
	<div class="d-flex justify-content-center">
		<a href="#" rel="nofollow" id="btn-edit" data-edit-id="{{ ocup.id }}" onclick="editar(this);"><i class="fas fa-edit"></i></a>
		{{ include('ocupacion/_delete_form.html.twig', {'id': ocup.id}) }}
	</div>
</td>
