<form method="post" action="{{ path('app_user_edificios_delete', {'id': user_edificio.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user_edificio.id) }}">
    <button class="btn">Delete</button>
</form>
