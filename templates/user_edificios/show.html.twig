{% extends 'base.html.twig' %}

{% block title %}UserEdificios{% endblock %}

{% block body %}
    <h1>UserEdificios</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user_edificio.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_user_edificios_index') }}">back to list</a>

    <a href="{{ path('app_user_edificios_edit', {'id': user_edificio.id}) }}">edit</a>

    {{ include('user_edificios/_delete_form.html.twig') }}
{% endblock %}
