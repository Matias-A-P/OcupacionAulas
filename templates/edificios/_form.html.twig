<div class="container">
{{ form_start(form) }}
    {{ form_row(form.edificio) }}
    {{ form_row(form.Sede) }}
    {{ form_row(form.facultad) }}
    <button type="submit" class="btn btn-primary">Guardar</button>
{{ form_end(form) }}
{# , {'render_rest': false} #}
</div>
