{% extends 'base.html.twig' %}

{% block cardheader %}
	<div class="col-sm-8">
		<h5>Cátedras</h5>
	</div>
    <div class="col-sm-4">
        <a href="{{ path('catedras_new') }}" class="btn btn-sm btn-outline-secondary" role="button">Agregar Nueva</a>
    </div>
{% endblock %}
{% block cardbody %}
    <table class="table table-sm table-striped table-borderless">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>acciones</th>
            </tr>
        </thead>
        <tbody>
        {% for catedra in catedras %}
            <tr>
                <td>{{ catedra.id }}</td>
                <td>{{ catedra.nombre }}</td>
                <td>
                    <a class="btn btn-outline-primary btn-sm" role="button" href="{{ path('catedras_show', {'id': catedra.id}) }}">Ver</a>
                    <a class="btn btn-outline-secondary btn-sm" role="button" href="{{ path('catedras_edit', {'id': catedra.id}) }}">Editar</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">sin registros</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}
